# Copy to .env for local development. Both values have working defaults.

# Where the FastAPI backend is listening. The dashboard falls back to bundled
# data if it cannot reach this, so leaving it unset is fine for a static demo.
VITE_API_BASE=http://localhost:8000
